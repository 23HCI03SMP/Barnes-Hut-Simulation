name: Build C++ Program

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - uses: msys2/setup-msys2@v2
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          # install GCC, GSL and OpenMP
          msys2 -c "pacman -S --noconfirm mingw-w64-x86_64-gcc mingw-w64-x86_64-gsl mingw-w64-x86_64-openmp" 
      - name: List files in the repository
        run: |
          dir D:/a/_temp/msys64/mingw64/include/gsl
      - name: Build
        run: |
          # make a bin directory 
          # and build main.cpp in the ./src folder
          mkdir ${{github.workspace}}/bin
          g++ -std=c++17 -v ${{github.workspace}}/src/*.cpp -o ${{github.workspace}}/bin/main.exe -I D:/a/_temp/msys64/mingw64/include -l D:/a/_temp/msys64/mingw64/include -lgsl -lgslcblas -lm -fopenmp
      - name: Run
        run: |
          # run the program
          ${{github.workspace}}/bin/main.exe