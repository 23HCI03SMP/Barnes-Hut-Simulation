#include "include/octree.h"
#include <iostream>

int main(){
    int boxX;
    int boxY;
    int boxZ;

    std::cin >> boxX;
    std::cin >> boxY;
    std::cin >> boxZ;
    int box[3] = {boxX, boxY, boxZ};

    return 0;
}